# 首版平台生产发布记录

发布日期：2026-09-08。平台发布编号：`20260908T124713Z`。状态：已上线并完成主代理的真实生产 SSO 读取验收。

## 部署范围

- 桌面：`https://desktop.meta-gravity.com`，与 Files 服务及个人中心、应用管理、文件、身份管理静态应用位于 1.12.253.86。
- 身份中心：保持 `https://identity.meta-gravity.com` 和原服务器，先行完成本轮迁移与升级。原 27 条应用映射逐项保留；3 位旧 active 管理员按既有权限迁入 `platform-admin`。
- Token One：原服务器 1.12.253.86，版本 `20260908T124729Z`，三个入口分别授权，业务 MySQL、原域名、凭据与额度保留。
- 指标知识库：原服务器 43.139.78.226，版本 `stage12-20260908-205200`，保持业务 PostgreSQL、原域名与体系权限，排除未授权的自动指标编码变更。

生产测试账户当时具有的应用目录为 files、personal-center、expert-database、token-one、identity；没有为验收自动增加 token-one-console、token-one-docs 或 app-manager 授权。入口存在与用户获得授权是不同状态。

## 验证结果

主代理通过生产既有 SSO 会话进入桌面，成功读取 Files 根目录、Token 门户、知识库体系列表、identity 管理概览。没有为这些验收改动业务数据或权限。HTTPS、未登录访问拒绝、PDF MIME 检查通过。

业务服务独立检查：Token 公网 live/ready 返回 200，SSO 模式为 unified，三固定 me 端点未登录均返回 401；知识库主页及 JS/CSS、health 返回 200，SSO 模式为 unified，未登录 me 返回 401。

Token HTTPS 站点与知识库 `/knowledge-base-inside/`、其 `/api/` 路径均设置：

```text
Content-Security-Policy: frame-ancestors 'self' https://desktop.meta-gravity.com
```

配置修改前已备份，`nginx -t` 通过后 reload，curl 确认生效；Token 原 HSTS 保留，知识库站点根路径与其他站点头部规则不变。

## 业务备份与回滚依据

| 应用 | 备份目录 | 内容 |
| --- | --- | --- |
| Token One | `/opt/mg-gateway/backups/20260908T124729Z-prepare` | 业务 SQL dump、原 Compose/env/镜像 ID、Nginx 原配置 |
| 指标知识库 | `/opt/mg-expert-database/backups/stage12-20260908-205200-prepare` | PostgreSQL custom dump、原 env/systemd/key、web 备份、旧 release 指针、Nginx 原配置 |

Token 当前镜像为 `mg-gateway:20260908T124729Z`，知识库 current 指向 `/opt/mg-expert-database/releases/stage12-20260908-205200`。归档路径、SHA256、测试范围及具体镜像 ID 见 [业务发布记录](./business-release-20260908.md)。回滚遵循 [部署说明](./README.md) 的认证兼容规则，禁止关闭鉴权或回退到绕过统一认证的旧行为。

本轮业务配置只增加可信 `DESKTOP_ORIGIN` 和 unified 模式；未运行 Token/知识库业务数据库迁移，未修改额度或业务角色。生产凭据仅保留于私密文件，SSH 使用已授权 1Password Agent，连接复用成功，未导出私钥。

## 后续独立补丁

用户后续提出的企微头像和全屏按钮正在另行实现与补丁发布。本记录不将这两项列为首版已上线功能，其最终状态应由后续补丁记录确认。
