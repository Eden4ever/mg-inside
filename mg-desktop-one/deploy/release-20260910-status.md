# 全平台发布核对

目标：完成应用目录统一到内核及区划、机构、范围授权等调整，完整发布并逐项验收。

发布包：`20260910T012500Z`。此表为执行状态，不是发布完成证明。

| 系统 | 发布单元 | 当前状态 |
| --- | --- | --- |
| 平台内核、桌面 | Java 镜像、桌面前端 | 镜像已构建；生产新目录已导入；金丝雀目录读取失败待处理；未切换 |
| 统一认证中心 | 身份镜像、登录页、管理页、数据库迁移 | 镜像已构建；待迁移及切换 |
| 应用管理 | `/apps/app-manager/` | 前端已构建入包；未切换 |
| 服务管理 | `/apps/service-manager/`、内核服务治理 | 前端已构建入包；待数据库结构及契约核对 |
| 个人中心 | `/apps/personal-center/` | 前端已构建入包；未切换 |
| 文件 | `/apps/files/`、文件服务镜像 | 前端及镜像已构建；未切换 |
| Office One | `/apps/office-one/`、文件服务中的 Office API | 前端已构建入包；待 Office 引擎兼容验证 |
| 资源管理 | `/apps/resource-manager/`、独立资源服务 | 前后端已入包；独立服务待切换 |
| Token 门户、控制台、文档 | 独立 Token 服务和静态发布 | 不在平台包；待比对当前源码与线上版本并构建发布 |
| 指标知识库 | 独立知识库 API 与静态发布 | 不在平台包；待比对当前源码与线上版本并构建发布 |
| 低空驾驶舱 | 外部应用 | 待核查接入及本次变更范围 |
| Document One | 独立文档服务 | 内核登记为待配置；统一认证真实联调及生产入口尚未完成 |

## 实际切换记录

内核、身份中心、文件服务和平台静态目录已切换至 `20260910T012500Z`，三个容器均 healthy。上表尚未逐项更新，不代表最终验收结果。

身份迁移命令直接作用于生产库，未按计划先演练隔离副本，且迁移时旧服务尚未停写。这是执行偏差。随后已停止旧桌面及身份服务，保存 `identity-after-migration.dump`，并切换匹配的新镜像。迁移前备份位于 `/opt/mg-desktop/backups/catalog-20260909T173539Z`；备份可读，但账号及授权逐行对比仍待完成，不得宣称无数据变化已得到验证。

已确认：HTTPS 证书验证通过，桌面及身份健康接口返回 200；身份容器使用独立密钥读取内核目录成功，共 14 条；`ApplicationReference` 仅有 `clientId` 列。Document One 和 desktop-one 无运行入口。Token 三入口、知识库、低空站点返回 200 的初步检查使用了跳过证书校验的请求，不能代替 TLS、SSO 或业务验收。

仍需完成：完整账号授权对比、生产真实登录和范围权限验收、资源后端发布、独立 Token 和知识库源码比对及发布、低空及 Document One 接入范围核查。正式回滚必须考虑已迁移身份表与旧镜像不兼容，不得只恢复旧镜像。

所有应用定义仅在内核维护。身份库迁移后仅保留应用标识外键及授权关系；不得建立第二套应用名称、启用状态或入口维护数据。

## 统一桌面移出应用目录

2026-09-10 发布身份镜像 `mg-identity-service:20260910T054827Z`。`desktop-one` 仅保留为统一桌面登录和机器调用的 OAuth 客户端标识，不再作为内核应用或身份授权应用。

生产已删除内核 `desktop_applications` 的 1 条 `desktop-one` 记录及其 2 条审计记录，并删除身份库 `ApplicationReference` 的 1 条引用；其用户授权、角色授权、范围授权、目录快照和 OIDC 会话引用均为 0。内核库备份位于 `/opt/mg-desktop/backups/desktop-client-removal-20260910T054827Z/kernel-catalog.dump`，身份库备份位于 `/opt/mg-desktop/backups/identity-20260910T054827Z/database.dump`。

清理后身份容器重启健康，公网身份健康页和桌面首页均返回 200。真实生产账号完成桌面 SSO、授权码交换、会话读取和退出；认证中心完整应用目录为 13 个，包含 `document-one` 且不包含 `desktop-one`。平台控制面 API 的台账提供方已改为 `platform-kernel`，已移除完成历史任务后不再需要的待登记工具。
