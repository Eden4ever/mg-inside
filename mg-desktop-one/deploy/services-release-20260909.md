# 服务出口分批发布记录

## 版本生命周期：最终 20260908T210250Z

首次 `20260908T205809Z`，补齐旧快照导入限制和跨域生命周期响应头后发布 `20260908T210250Z`。最终桌面 current/镜像 healthy，六个启用版本和部署绑定不变，13 个历史版本及既有记录完整保留。

- 新登记为草稿，首次环境发布转为已发布；弃用保留现有调用并记录迁移说明、计划退役日期，拒绝新增/更换绑定；退役必须所有环境均不再使用，保留契约且不可恢复。日期不自动触发停服。
- 全局状态表 `service_version_lifecycles` 与不可变契约分离，独立修订；运行账号拥有 SELECT、INSERT 和 value 列 UPDATE，无 DELETE。跨环境退役检查与发布共用事务锁，旧快照不能绕过当前全局生命周期。
- 42 项单测、类型检查、服务管理前端构建与独立 PostgreSQL/真实桌面 HTTP 综合测试通过。覆盖全生命周期、修订保护、日期、重启、跨环境使用阻止退役、并发发布与退役仅一个成功、旧快照导入拒绝、弃用响应头及原断库恢复。业务路由使用替身的部分不等同全量业务回归。
- 正式页面验证公共字体、生命周期表单、未提交保护、变更确认及状态提示；未启用的 Office 1.0.0 历史版本弃用后已恢复维护，生命周期 revision=2，两条验收审计可读。未对生产版本执行不可恢复退役；当前 Office 1.1.0 和其他五个服务保持启用。
- 最终版本六服务正式只读调用全部成功；数据库核验不存在草稿/退役版本仍被启用的记录，契约摘要一致。
- 初次包 SHA-256 `ee953525491b63d90d6f5157cb196910bbacfecf7ab2990e3392dc0e00e60553`；最终包 `cdfb7f4847cbf2f422c701d757fbdc9f9602fdd51361efc9a1700516a2846bcb`。
- 备份目录 `/opt/mg-desktop/backups/service-governance-20260908T205809Z`、`service-governance-20260908T210250Z`；SQL 压缩备份 SHA-256 分别为 `4b0af2101623f5aeb5ee0c222196e6a8768beceb09ae8c011468b06b6721b56d`、`5b6eadc62b35abae6f0eb3f097873489751fc84d9eb7107e3443cb4ed3960ce3`。本批未执行生产程序回退。

规则及断库/回退边界见 [service-lifecycle.md](../docs/service-lifecycle.md)。下一批契约结构兼容分析；健康过期、依赖、公共能力、机器身份及发布集成仍保持原目标。

## 环境发布与受控部署绑定：最终 20260908T204230Z

首次绑定发布 `20260908T203920Z`，随后正式页面验收发现公共客户端拒绝根路径的环境查询参数，修复并发布 `20260908T204230Z`。最终镜像与 current 均为 204230，健康检查通过；六个服务的现有版本和上游地址保持，绑定到核验过的实际运行产物。

- 环境分别保存启用版本、修订、绑定和记录。管理发布要求明确环境、endpointRef、部署摘要和修订号；业务出口固定使用所在实例环境。后台在数据库锁内重读受控登记，不接受页面任意上游地址。
- 生产配置只登记实际生产部署。Files 和 Office 关联同一 Files 运行产物，Office 保留 `/api/office` 前缀；Token 仍用旧机原网关。配置由 root 持有并只读挂载，发布工具的配置更新保留现有有效绑定，使用原始摘要防止覆盖其他发布。
- 本批完整 39 项桌面测试、类型检查、服务前端构建和独立 PostgreSQL/双桌面 HTTP 综合测试通过；随后公共客户端 5 项测试通过，包含新增环境查询回归，共覆盖 40 个不同单测。综合测试验证真实数据库环境隔离、版本/部署切换、过期修订/摘要、未绑定拒绝、受控配置发布及恢复；路由目标为可辨识 HTTP 替身，不代表全部业务接口回归。
- 正式六服务只读调用成功。新版页面已核验 Office 实际提供方、发布二次确认、相同绑定重试、刷新回读、历史版本无匹配提示、变更前后部署列及公共字体布局。重复发布后再次核对数据库，六个修订均仅比首次绑定前增加 1；13 个历史版本和全部原审计、调用记录保留。
- 重新捕获六个实际进程/容器的产物，与受控登记逐 endpointRef 比较一致。比较忽略数据库返回的数组顺序；不把一次核验当作持续运行漂移检测。
- 首次包 SHA-256：`075c4c74cd42b70f806869407be0e44d2679ca822b68a028029e6dd5bee71d90`；最终包：`00cbe474bd865c0b8889195af3267db9a63172cde798bc9c105aad62fd5fc53b`。
- 备份：`/opt/mg-desktop/backups/service-governance-20260908T203920Z` 与 `service-governance-20260908T204230Z`。数据库压缩备份 SHA-256 分别为 `6db4ec999ec86f13c01abc3becbb0cb6574bc8740bcf38c75d338ae2fd15ab6b`、`214764b47530d28219d08fdac2dba333e3767a465da959aa5bbfaa1a0a01a022`。本批未执行生产程序回退。

操作指南：[环境发布与部署绑定](../docs/service-environments.md)。下一批生命周期弃用/退役，后续兼容推导、健康过期、依赖、公共能力、机器身份与发布 SDK 保持原范围。

## 独立数据库批次：20260908T195300Z

服务目录正式迁至新机独立 PostgreSQL 数据库 `mg_services`，专用容器 `mg-service-registry-db-1`，运行账号 `registry_app`；只监听宿主 loopback 15440，未改动身份和其他业务数据库。

- 桌面 current/实际镜像 `20260908T195300Z`，与数据库均 healthy。发布包 SHA-256 `e4c9e5238df051bf10fabfa7e2fea747a4d20c0344662cb5420354a1d7ed2f9a`。
- 迁移源摘要 `5310103efb1d04d94c3348c56aa76ec14b111d02e24b0e740e708272b2f10798`；13 个历史版本、6 个启用状态、14 条变更和全部原有调用记录保留。迁移后六服务正式只读调用成功，数据库已有 46 条调用、generation 为 6（本批核验时）。
- 运行账号无超级用户、建库/建角色或建表权限，无旧版本更新/删除权限。版本、环境绑定、变更与调用记录分别存表；写入在数据库事务中协调多个实例，保留修订号与不可变校验。
- 最后确认的路由快照用于断库期间已有业务调用；认证、应用和对象权限仍由原链路校验，管理读写要求数据库在线。断库期间调用记录可能缺失，数据库恢复后自动读取新目录。
- 36 项桌面测试、类型检查、构建及 1 项独立数据库/真实桌面 HTTP 综合测试通过。覆盖迁移、双实例并发、testing/production 隔离、受限账号、实际断连、快照重启、管理 503、匿名及方法拒绝、恢复、导出回原 JSON 引擎。
- 备份目录 `/opt/mg-desktop/backups/service-storage-20260908T195300Z`；`services.dump` SHA-256 `d6c61b3a9465fe6e57251db14547f9db47fcf4bcac5baaa9951ae6e63088810b`。已实际恢复到唯一临时数据库并校验历史记录，临时库已删除；生产没有执行程序回退。
- 服务管理页面新增当前运行环境提示，继续公共壳和既有字体布局。六份契约直接从 PostgreSQL 核验，摘要与源码一致。通用后续发布脚本已增加真实数据库备份。

下一批继续环境发布与受控部署绑定，随后按原计划推进生命周期等；当前数据库环境字段不代表完整环境治理。操作与故障边界见 `../docs/service-storage.md`。

## 统一身份资料契约批次：20260908T193100Z

已登记启用 `identity.api 1.1.0`，覆盖 `GET /auth/me`。至此六个已有服务的 29 个登记操作均有 OpenAPI 契约；不代表未登记业务接口或完整服务治理已完成。

- 桌面 current/实际镜像 `20260908T193100Z`，健康检查通过；包 SHA-256 `138649e14d554f9e549306de695df3d5c9f1915bb59dca62fb81521b739dae7c`。
- 备份 `/opt/mg-desktop/backups/service-governance-20260908T193100Z`，前一桌面目录/镜像 `20260908T191600Z`。仅发布桌面核心及服务管理静态资源；身份业务接口和前端未改动，沿用原进程。
- 明确提供方基础本人资料读取与现有管理出口授权的区别；仅有效会话本人可读，管理出口继续要求 identity 应用授权与平台管理员角色。CSRF 值不进入示例或调用日志，未开放任意用户查询。
- 桌面 36 项测试、类型检查和生产构建通过。身份综合测试使用独立 PostgreSQL Schema、真实密码登录、Nest 控制器与 Guard，覆盖两账户、实时角色/授权变化、会话撤销/过期/安全版本/停用及敏感字段排除；测试 Schema 已清理。未把它宣称为 OIDC 或第三方登录全链路回归。
- 正式登记、启用及本人资料只读出口调用成功；六份生产持久化契约摘要与源码一致且当前启用。正式契约页面视觉核验通过，身份应用既有公共壳与页面标题实现已核查。

下一项：独立服务管理数据库/账号、迁移及回滚验证、显式环境绑定。生命周期、兼容性推导、依赖观测、公共能力和机器身份仍保持原规划待办。

## Token 契约与公共标题批次：20260908T191620Z

已登记启用 `token-one.api 1.2.0`，覆盖账户模型和公开模型两个目录。旧 1.1.0 清单保留；提供方原始公开接口仍匿名可读，统一出口和 Token 三入口授权边界保持不变。

- 桌面 current/实际镜像 `20260908T191600Z`；核心包 SHA-256 `1232b534787a4f5dbb85b919b083534688e41ff486e033547daf447bcef1355b`，健康检查通过。
- Token 静态 current：`/var/www/mg-token-one-services/releases/20260908T191620Z`，位于现有 `1.12.253.86`；包 SHA-256 `b450170111de4b910c700b4fd40d80dc9c17d4c1742623709043b45b96694dda`。
- 网关镜像仍为 `mg-gateway:20260908T124729Z`，本批仅更新静态前端和桌面服务契约；没有重启模型网关、修改 Nginx 或迁移业务数据库。静态脚本复制当前完整资源，保留旧页面依赖，校验后原子切换，HTTP 失败时恢复前一入口。
- 核心备份 `service-governance-20260908T191600Z`；Token 备份 `/opt/mg-gateway/backups/frontend-services-20260908T191620Z/release.json`，记录前一静态目录 `20260908T173200Z`。
- 门户、控制台、文档使用公共标题函数；错误处理兼容网关嵌套业务错误和出口平铺错误。布局继续使用公共壳/阅读页头，导航位置保持原位。
- 桌面 36 项测试、Token 契约 HTTP 综合测试和三入口授权测试通过；前端及核心类型检查、生产构建通过。Token 测试使用实际控制器/Guard/分组服务，身份内省和仓储为受控替身，未将其称作数据库回归。
- 生产公开目录 14 个模型的实际 JSON 符合 1.2.0 契约，公开字段排除了分组、价格、绑定和上游信息。正式两个统一出口调用成功；五份生产契约摘要与源码一致且已启用。
- 正式门户显示 14 个可用模型，控制台模型管理与文档目录加载正常；标签分别为“概览 · Token One”“模型管理 · Token One 控制台”“模型目录 · Token One 文档”。本轮没有发起付费推理或修改模型、分组、令牌。

这一轮资源、文件、Office、知识库、Token 已按序发布。统一身份资料服务仍只有清单，需补真实契约；服务管理独立数据库、环境绑定、生命周期、完整兼容性判定与公共能力推广仍按原目标继续。

## 知识库契约与创建保护批次：20260908T190645Z

已登记启用 `expert-database.api 1.1.0` 三个只读操作：体系清单、体系详情、语义库清单。普通账户保持体系授权范围，管理员保持全局可见；撤销授权后相同会话的查询立即不再返回原体系。契约记录无版本、无构建、最近构建与生效构建不同等实际语义。

- 桌面 current 与实际镜像：`20260908T190350Z`，healthy；核心包 SHA-256 `198e1cadd0f26dbe1f9c3b179a10d923ab9afbf57e58c6f54a0f8fd044ad6c06`。
- 知识库前端最终快照：`/opt/mg-expert-database/frontend-releases/20260908T190645Z`；最终包 SHA-256 `ceebc7e2eecb28fcf3ddb1b108231f3688d1a4e9174f28e8a94ee97246144b12`。先发 `190400Z` 表单保护，再在 `190645Z` 补齐公共页面标题。
- 核心备份 `service-governance-20260908T190350Z`；知识库静态入口备份 `frontend-services-20260908T190645Z`。业务 API 仍运行 `stage12-20260908-205200`，本批未部署数据库或后台业务变更。
- 修复新建体系提前关闭：成功才关闭，失败在弹窗中保留输入并重试。体系和语义库创建期间禁止取消、遮罩关闭和 Escape；填写后取消使用公共确认。相关页面导航和桌面关闭保留对话框/请求保护。体系卡片使用体系 ID 作为稳定键，兼容无版本情况。
- 页面继续使用公共壳、字体和既有 18px 一级标题规范；浏览器标签通过公共函数显示“指标体系 · 指标知识库”或“智能语义库 · 指标知识库”，导航位置保持原位。
- 桌面 35 项测试、知识库相关前端 18 项测试、1 项独立 PostgreSQL/真实登录/HTTP 综合契约测试通过。桌面及知识库前后端类型检查、前端和核心构建通过。综合测试覆盖三操作及双账户授权、管理员范围、撤销、空状态和 401/403/404；测试 Schema 已清理。
- 正式两个无参数只读出口调用成功；四服务契约摘要与源码完全一致且都已启用。正式页面加载 2 个体系、1 个语义库，未提交测试业务数据；新建放弃确认和最终公共标题已核验。

下一批为 Token 契约和页面核查；独立数据库、环境绑定、完整兼容性判定等原规划未完成项继续保留。

## Office 契约与公共页面批次：20260908T184200Z

已登记启用 `office-one.api 1.1.0`，覆盖六个操作。资源、文件、Office 三份契约的生产持久化摘要均与源码一致。Office 实际提供方保持 Files `/api/office`，应用授权仍为 Office；签名下载和回调不作为公共操作。

- current：`/opt/mg-desktop/releases/20260908T184200Z`；桌面实际镜像 `mg-desktop-service:20260908T184100Z`，Files 保持 `mg-files-service:20260908T171500Z`，两者 healthy。
- 核心包 SHA-256：`2b7f3d6979d15a084103070f588a8dc2b57ca745f6b5755d4aa50400df2531c0`。
- Office 静态包 SHA-256：`57ed2477793f2effc52d86977f39685bcabfff937208726d28e4028264e384fa`。
- Office 使用公共未保存弹窗及浏览器标题；保存/创建期间阻止离开，并发离开入口合并一次确认，创建成功跳转和重试经过保护。
- 桌面 35 项、Office 4 项测试及类型检查、构建通过。真实 HTTP 契约验证包含六操作、双账户隔离、签名拒绝、签名回调落盘与保存请求确认；编辑引擎使用受控替身。
- 本地真实 ONLYOFFICE 新建 Word、输入 `OFFICE-CONTRACT-20260909`、取消离开、实际保存通过；下载 DOCX 含标记、版本为 3，单个本地测试文件已清理。下载证据保留在 `.runtime/office-contract-ui-705ce96e-6257-43d8-84f2-3dd4dcf130d2.docx`。
- 正式服务管理文档查询调用成功；已有 QA DOCX 只读编辑器加载正常，返回后最近打开列表将该文件列于首位，打开时间为 2026-09-09 02:47:26。未编辑正式文档内容。

下一批为知识库契约和公共页面核查。后文保留历史批次，不代表当前部署版本。

## 接口契约批次：20260908T182800Z

已先发布服务管理契约能力，再依次登记启用 `resource-manager.api 1.1.0`（3 个操作）和 `files.api 1.1.0`（14 个操作）。最后发布初始化、源码同步，以及同名业务字段和认证方案变化的差异比较补充。现有提供方接口不变，目录升级未重启 Files、Office 或资源后端。

- 桌面 current：`/opt/mg-desktop/releases/20260908T182800Z`。
- 桌面实际镜像：`mg-desktop-service:20260908T182800Z`，healthy；Files 保持 `mg-files-service:20260908T171500Z`，healthy。
- 最终发布包 SHA-256：`6d8555082c7f2b9203a27ee55aa47f76d7d3196d88e28d83f46492990e5bacde`。
- 备份：`/opt/mg-desktop/backups/service-governance-20260908T182800Z`；包含发布前目录与镜像信息。不自动恢复业务数据快照。
- OpenAPI 3.1.1 结构校验、JSON Schema 2020-12 编译、清单完整绑定与不可变摘要、文档浏览与下载、登记包导入、契约变化审阅保护已上线。
- 两份契约在提供方 `services/openapi.json`，清单/登记包/公共目录/初始契约目录一致。`scripts/verify-contract-deployment.py` 确认生产两份摘要与源码相同、当前版本已启用。
- 34 项桌面测试和生产构建通过；新增真实 HTTP 测试覆盖资源 3 个、文件 14 个操作。真实本地身份链路、账户偏好隔离、Office 打开记录、匿名/方法/路径/CSRF 拒绝均通过。正式资源概览与文件列表查询成功，最终浏览器复核两个版本仍已启用。

本批 `181700Z` 前端曾因根路径构建而出现空白，已先回退静态入口、在 `182100Z` 修复并验证恢复。现有专用生产构建脚本与打包路径断言阻止同类构建进入发布包。最终以 `182800Z` 为准。

完整 JSON Schema 兼容性推导仍未完成：当前结构变化保守要求审阅；没有历史契约或超出复杂度上限时不会宣称兼容。后续继续 Office、知识库、Token 契约接入及原规划其余公共能力。


后续更新：服务版本治理已于 `20260908T174700Z` 发布。桌面 current 与运行镜像均更新到该版本；新增操作差异展示、启用修订号校验、不兼容操作变化确认和启用时路由重叠检查。28 项桌面测试及本地真实 HTTP 检查通过；正式页面验证 Token 1.0.0 → 1.1.0 的新增接口。备份 `/opt/mg-desktop/backups/service-governance-20260908T174700Z`，包含前一发布信息与目录快照。下表保留首轮批次历史。

2026-09-09 已按服务管理 → 资源管理 → 文件 → Office → 知识库 → Token 的顺序发布。

| 批次 | 版本 | 交付 |
| --- | --- | --- |
| 服务管理 | 20260908T170000Z，前端最终 20260908T173400Z | 目录、注册、启停、调用出口、活动记录、独立图标；规范页面和字体；导航恢复原位；桌面启动器去白底框；账户切换偏好保护 |
| 资源管理 | 20260908T170700Z，前端最终 20260908T171813Z | 3 个接口接入、公共请求客户端、未保存保护、过期查询取消、14px 正文 |
| 文件 | 20260908T171200Z，前端最终 20260908T171833Z | 已登记接口及上传走统一出口；显示模式缓存按账户分键；公共字体和主题色 |
| Office | 后端 20260908T171500Z，前端最终 20260908T171853Z | 请求客户端接入；全部文档/最近打开；按账户记录打开时间，保持内容版本与修改时间 |
| 知识库 | 20260908T172200Z | 3 个公共查询走出口，保留业务权限与响应协议，标题字号/主题色对齐 |
| Token | 20260908T173200Z | 账户模型查询经统一出口；模型服务 1.1.0 已登记启用；前端独立静态部署，网关未重启 |

桌面 current：`/opt/mg-desktop/releases/20260908T173400Z`。桌面运行镜像仍为 `mg-desktop-service:20260908T170000Z`，Files 运行镜像为 `mg-files-service:20260908T171500Z`；后续静态批次只建立兼容镜像标签，不重启业务进程。

知识库静态页面：新服务器 `/var/www/mg-expert-database/knowledge-base-inside`，构建快照 `/opt/mg-expert-database/frontend-releases/20260908T172200Z`，旧 index 备份在 `backups/frontend-services-20260908T172200Z`。

Token 仍在原服务器 `1.12.253.86`：Nginx 从 `/var/www/mg-token-one-services/current` 提供前端，`/api`、`/v1` 仍代理原网关，原 `/api-docs/` 代理保留。后续 Token 前端发布须更新这个独立静态入口，不能只更换容器 web-dist。备份 `/opt/mg-gateway/backups/frontend-services-20260908T173200Z`。回滚前端入口可恢复该备份 nginx.conf，执行 nginx -t 后 reload；无需重启网关。

桌面静态回滚可将 current 原子切回上一完整发布目录。服务管理首版的身份配置、桌面环境、Nginx 和数据库快照在 `/opt/mg-desktop/backups/service-manager-20260908T170000Z`。Office 后端回滚使用此前镜像；不自动恢复业务数据快照，以免覆盖发布期间的正常编辑。

账户隔离：桌面排序、Dock 固定项和布局以用户 ID 存储。旧页面切换账户后会重新初始化，偏好请求携带账户绑定，服务端拒绝与登录账户不符的读写。文件显示模式使用账户专属缓存键，不迁移无法确定归属的旧公共键。Office 最近打开由文件所有者过滤；成功创建打开会话时更新 officeAccessedAt，不回填无法证明的历史，回收站条目不返回。

验证：桌面/服务客户端 25 项测试，Files/Office 后端 14 项测试，Office 保存状态 2 项测试，知识库相关 5 项测试；六份应用清单与公共目录一致，当前 29 个操作。真实登录接口验证覆盖旧账户偏好请求拒绝、文件上传/下载/访问及清理、Office 新建/打开/历史记录及清理、CSRF、匿名、路径和方法拒绝。正式页面验证服务目录、资源只读调用、Office 测试文档预览与历史、知识库体系和 Token 模型列表。

当前边界：只有登记的接口走统一出口，未登记业务接口仍走原代理；完整 OpenAPI 契约、机器身份与多副本存储尚未实现。服务目录是平台公共能力目录，业务数据仍由提供应用按用户、角色和对象权限校验。
