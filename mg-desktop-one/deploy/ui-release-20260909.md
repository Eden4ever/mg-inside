# 公共壳返回入口、应用版本与 Office 字体

本批公共壳样式引入桌面嵌入空白，已由 [嵌入修复批次](./embedded-fix-20260909.md) 修复并替代下表版本。下文保留初次发布记录；初次独立网页验收不能证明桌面嵌入正常。

2026-09-09，按应用逐个构建、摘要校验与发布。

| 应用 | 当前前端版本 |
| --- | --- |
| 服务管理 | 20260909T015833Z |
| 资源管理 | 20260909T015932Z |
| 文件 | 20260909T020037Z |
| Office One | 20260909T020113Z |
| 个人中心 | 20260909T020148Z |
| 统一身份管理 | 20260909T020226Z |
| 指标知识库 | 20260909T020346Z |
| Token 门户、控制台、文档 | 20260909T020439Z |
| 应用管理 | 20260909T020559Z |

公共 `ApplicationHeader.vue` 在独立网页顶部右侧增加“回到桌面”链接，目标来自 SDK 配置的可信桌面来源。窄屏保留图标与中文可访问名称；桌面内嵌隐藏该入口，现有导航位置未调整。普通链接遵循浏览器离开事件，Office 既有未保存 `beforeunload` 保护继续生效。

应用目录通过受授权过滤的 `/api/applications` 显示当前前端构建标识。服务端仅读取平台登记应用的公开 `version.json`，不请求个人外链、不携带凭据；设置 2.5 秒超时、4KiB 上限和 15 秒缓存。缺失或无效元数据标为“未登记”，个人外链显示“外部网站”。Token 三个入口共用同一部署版本。版本不是接口契约版本，也不是各项目默认 package.json 版本。

逐应用、服务治理、Token、知识库打包器以及完整发布脚本自动生成 `version.json`，包括 appId、发布标识与 index.html SHA256；元数据进入构建摘要校验。后续发布持续更新。新脚本 `scripts/publish-header-app.py` 逐个执行构建、发布和 HTTPS 产物核验，逐项记录在 `artifacts/header-app-releases.jsonl`。

桌面运行镜像 `mg-desktop-service:20260909T015833Z`；静态 current 为 `/opt/mg-desktop/releases/20260909T020559Z`。后续六批仅静态发布，运行镜像名称与静态 current 不同是预期行为。桌面服务发布包 SHA256 为 `92d8d7aedfecf8722ea179f2fd825f127454d44a9eef867b7b0c1d2612fd0077`；数据库备份和旧镜像记录在 `/opt/mg-desktop/backups/service-governance-20260909T015833Z`。服务数据没有迁移或重新绑定。

知识库前端发布包 SHA256 为 `1b3eb3c8e833a6b80f1184481a543f59d75c87936ee972a72514b3f63226da73`，保持原 API 与数据库。Token 前端发布包 SHA256 为 `c37c5ff68104ef9fc63e725c3c207d1ad0a886129383d8d1d345236284c9a3b7`，仍在旧服务器，网关镜像与启动时间未变：`mg-gateway:20260908T124729Z`、`2026-09-08T13:05:55.162580004Z`。

验证：桌面 48 项单测和类型检查通过，所有应用前端类型检查及生产构建通过；线上版本元数据和 index 摘要核验通过。各独立页面按钮可见且目标正确，服务管理真实点击后到达统一桌面；Token 门户、控制台与文档三个入口均单独核验。正式应用目录 11 个入口全部显示与各线上部署匹配的版本号，无“未登记”项。Office 字体安装及实际 PDF 排版验证见 [字体记录](../docs/office-fonts.md)。

这批交付是用户追加的界面与文档字体需求。服务契约完整兼容推导仍未接入：本地 schema-compatibility.ts 与四项测试属于未发布基础模块，原服务治理目标的其他未完成项继续保留。
