# 服务管理与公共能力目标核对

更新：2026-09-09。目标保持“沿约定顺序推进”，不以首批接口可调用替代全部规划验收。Token 桌面嵌入故障已修复，本回合推进并发布基本结构兼容推导、字段定位及方向性发布检查，分类为取得进展。后续仍须复杂结构兼容推导、健康过期、依赖及公共能力等整体验收。

| 要求 | 当前证据 | 状态与下一步 |
| --- | --- | --- |
| 先服务管理，再资源、文件、Office、知识库、Token，逐项发布 | deploy/services-release-20260909.md 及各发布脚本、正式调用记录 | 首轮顺序已完成；后续能力继续按此顺序验证 |
| 服务登记及统一出口，保留提供方授权 | server/services.ts、main.ts，真实登录验证脚本及提供应用权限测试 | 29 个操作已登记；未登记业务接口仍用原代理 |
| 公共壳、字体、正文、导航与独立业务图标 | 各应用已发布前端，公共 ConfiguredApplicationShell、页面样式；浏览器核验；ui-release-20260909.md 与 office-fonts.md | 追加完成网页顶部回到桌面、应用目录当前构建版本、Office 五种中文字体；后续每批仍须核查业务表单与窗口行为 |
| 应用排序、视图偏好、Office 最近打开按账户隔离 | App.vue 偏好账户绑定，Files 账户缓存，Office 后端两账户 HTTP 测试及正式记录 | 已实现并验证 |
| 清单不可变、幂等注册 | services.test.ts、持久化摘要校验 | 已实现 |
| 版本比较、旧页面并发保护、路由冲突防护 | services/versions.ts、7 项服务目录测试；正式版本比较页面 | 已发布 20260908T174700Z，覆盖操作列表/方法/路径，不代表参数与响应契约兼容性已完成 |
| 资源查询、文件元数据两份真实 OpenAPI 契约，以及后续应用契约 | 六应用 services/openapi.json、registration.json，桌面契约测试、知识库与统一身份独立数据库 HTTP 综合测试、Token 控制器与 Guard 测试；生产摘要与源码一致 | 已发布：资源、文件、Office、知识库和身份 1.1.0，Token 1.2.0；覆盖 29 个登记操作；正式只读调用通过。Token 仓储及身份内省为测试替身，不等同于业务数据库全量回归 |
| 离线契约校验、文档浏览、参数和响应兼容性检查 | 官方 OpenAPI Schema + Ajv2020、共享包含关系及组合比较、69 项桌面测试、构建后隔离 HTTP 发布测试及生产字段定位页面 | 最新已发布 20260909T025742Z：基本结构、allOf/anyOf/oneOf/not 及引用兄弟约束支持有界证明，排他条件不能被普通并集合并；循环引用、任意复杂约束及超预算仍须审阅。范围见 service-compatibility.md；身份是替身、JSON 临时存储，不冒充 PostgreSQL 回归 |
| 独立数据库与账号、正式迁移和回滚验证 | service-postgres.ts、service-snapshot.ts、verify-service-storage.ts、docs/service-storage.md；正式 13 个版本和全部原记录保留，真实数据库备份已在独立临时库恢复验证 | 已发布 20260908T195300Z；目录支持多实例事务，不等同于整个桌面多副本化。程序回退的数据恢复路径已在本地真实库验证，未在生产执行程序回退 |
| 版本与环境绑定、弃用/退役 | service-deployments.ts、service-lifecycle.ts、版本生命周期页面、独立状态表、42 项单测及真实 PostgreSQL/双环境 HTTP 回归；生产保留六绑定和 13 历史版本，Office 未启用历史版本弃用后恢复的审计可读 | 环境绑定与生命周期已发布。跨环境使用检查、发布/退役并发、日期、不可恢复退役及旧快照导入拒绝经隔离测试；生产未执行不可恢复退役。持续产物漂移、SDK 和健康过期未完成。见 service-environments.md、service-lifecycle.md |
| 目录故障不阻断已有业务调用 | 实际独立数据库断连和构建后的桌面 HTTP 测试；文件出口使用最后确认快照仍成功，登录和方法校验仍执行；管理读写返回 503 | 已实现；断库期间调用记录可能缺失，不用于完整计量；无有效快照时拒绝初始化猜测路由 |
| 健康信息过期、依赖与调用关系 | 当前为有限条数的已转发请求元数据 | 未完成，需真实观测语义；不能将目录可用视为上游健康 |
| 公共页面状态、表单关闭保护抽象及两个真实消费者 | 已有桌面 dirty/busy 与各业务保护；Office 公共弹窗、并发离开合并、忙态拒绝及真实编辑验证已发布，尚存在重复逻辑 | 部分完成；Office 本地 guard 不等于跨应用公共抽象，仍需普通表单和第二消费者验证后推广 |
| 文件选择器、任务、通知接入及模板检查 | 已有部分桌面消息/弹窗基础 | 尚未完成整体验收 |
| 机器身份、细粒度授权、注册 SDK 与发布集成 | 当前仅用户会话出口与管理员代登记 | 未完成；统一身份及提供应用共同实现后开放 |

完成目标之前，必须逐项取得当前源码、测试和生产行为证据；本文件不将未完成项重新定义为非目标。
