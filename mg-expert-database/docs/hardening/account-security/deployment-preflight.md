# 认证发布预检

2026-09-07，当前尚未切换生产应用版本。

> 状态更新：本文下方记录分阶段证据；最终已发布 `stage7-20260907-214100`，以“正式发布结果”为准。

## 已验证

- 生产现有服务 `mg-expert-database-api` 为 active，当前版本 `stage6-20260907-193230`。
- 生产架构 x86_64，`/opt` 剩余约 50 GB。
- 已安装项目独立 Node.js 24.20.0 到 `/opt/mg-expert-database/runtimes/node-v24.20.0-linux-x64`。安装脚本验证官方预置 SHA-256，运行版本检查通过；没有替换系统 Node.js，没有重启现有 API。
- `remote-deploy-stage1.sh` 经生产 Bash 的 `bash -n` 只读语法检查通过。
- `node scripts/test-auth-cutover.cjs` 通过：使用本地容器 Bash、将所有服务与文件操作替换为桩，验证切换后错误保持原退出码、停止 API、不执行旧版本链接恢复或文件变更。

## 失败处理

认证切换前失败仍可恢复原服务配置。切换后失败必须停止 API 并保留现场，不能自动恢复可能忽略 MFA 的旧版本。优先修复新版本；若人工回滚，必须确认目标版本支持当前认证策略和数据库结构，不能将已开启 MFA 的账号降为单因素。

数据库备份和 `auth-config.key` 必须配套保留。发布脚本在密钥存在时验证 32 字节并以 root 0600 备份。恢复数据库不会自动恢复密钥，人工恢复必须核对对应备份。

## 尚未完成

仍需完成认证剩余验收、生产配置检查、最新构建产物、存量数据迁移备份验证以及真实生产切换后的健康检查。上述预检不代表已经发布。

## 21:33 追加验证

- 生产配置的 WebAuthn origin 为 `https://yshj.meta-gravity.com`，生产模式开启；仅检查配置存在性，没有输出凭据。
- 生产已应用 15 个迁移，截至 `20260907005000_embedding_model_config`。
- 在生产主机保留备份 `/opt/mg-expert-database/backups/auth-preflight-20260907213314`，将备份还原到独立临时数据库，依次执行三个新认证迁移。演练通过，19 用户、0 默认开启 MFA、0 历史误绑撤销；知识记录、模块内容、内容修订、摘要修订及依据逐表内容哈希相同。
- 临时测试库已删除，随后只读查询确认不存在；生产服务仍 active，业务数据库未迁移。
- 候选包 `artifacts/stage7-20260907-213400.tar.gz` 已生成，大小 17,835,748 字节，SHA-256 `2eacf968870e759b1e3a82597fe0c233ed0ac3b73e81b0a606aa6540c119be06`。这是候选包，不是生产已部署版本。
- 该候选包已上传至生产主机 `/tmp` 并在上传完成后重新确认 SHA-256 一致；未执行发布脚本。随后调整了测试并发配置，正式发布前需重新打包当前工作树。
- 全量并行测试暴露出默认进程数争抢资源、解析子进程用例总预算小于其生产硬超时的问题。测试进程数限制为每包 2，单次解析测试预算 20 秒、三次解析集成用例 50 秒；生产解析仍保持每次 15 秒硬超时，未改变安全限制。

## 正式发布结果

- `pnpm test` 最终退出码 0：契约 4 项、API 75 项、前端 113 项全部通过。真实数据库多请求集成用例采用 20 秒用例预算，不是生产请求超时。
- 新包 `stage7-20260907-214100.tar.gz`，17,835,762 字节，SHA-256 `f32d6ccbf91f0be282aa7adacc969dd11d8368c3f76b99593da513c3b8ab4303`。
- 发布脚本退出码 0；锁文件安装、Prisma 生成及三个新迁移全部成功。切换后 API active。
- 正式切换前备份：`/opt/mg-expert-database/backups/stage7-20260907-214100`。
- 生产 current 指向 `/opt/mg-expert-database/releases/stage7-20260907-214100`；运行进程实际使用独立 Node.js 24.20.0。
- 数据库已完成 18 个迁移；19 用户，0 默认开启 MFA。
- 公网 `/knowledge-base-inside/systems` 及新 JS 资源 `index-BnuiPo_k.js` 返回 200，健康接口返回 200；未登录访问 `auth/me`、`account-security`、`mail-settings` 均返回 401。

真实邮箱投递、真实用户设备 WebAuthn 注册和生产登录后端到端操作尚未验证，不将这些公网只读检查冒充完整认证验收。原安全加固目标还有本地运行环境及最终证据收尾，保持进行中。

## 最终认证验收

用户完成实际操作并确认“正常安全密钥能用”后，再次只读核验生产：服务 active，1 个已验证邮箱、1 把安全密钥且已有成功使用时间，当前有效登录会话包含 `{local,key}`。邮箱绑定验证码确认及真实密钥登录已有生产证据，先前的外部验收等待已解除。

认证核心源码与 stage7-20260907-214100 发布产物逐文件 SHA-256 一致。本地数据库端口与默认密码问题已完成实际修复，详见 local-database-verification.md；原四项安全发现修复报告已归档。安全加固、认证改造和生产发布目标完成，不代表系统不存在其他未知漏洞。

另行请求的个人中心样式优化仍为本地修改，未包含在此次已发布认证版本中。
