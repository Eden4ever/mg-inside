# 指标内容确认删除

后端 `stage14-20260909-042939`，前端 `20260909T043046Z`，已发布至 `43.139.78.226`。

原先打开指标工作台就可能建立空的研究记录，而删除接口拒绝所有已建立记录的指标。现支持用户在删除确认框中确认后，删除没有下级节点的指标及其内容。

确认框说明删除范围：摘要、研究模块、关联依据、AI 建议、内容修订和研究分工。请求携带确认时的指标名称；后端在体系事务锁内核验权限、节点版本、子节点和确认名称，名称不一致或有内容但未确认时拒绝执行。带下级指标的节点继续要求先处理下级。

数据库沿用已有外键级联关系，无 Schema 迁移。删除与审计在同一事务中完成，审计保留指标名称、编码、层级、父节点和内容数量。历史语义库构建快照保持原有快照语义，此操作不会重写历史构建。

前端取消确认不发出删除请求；保存或编辑未完成时提示先处理当前编辑。删除成功刷新目录并退出已删除指标，失败保留原工作区。

验证：

- 后端编辑与删除 16 项测试、前端指标目录与应用路由 27 项测试通过。
- 前后端类型检查和生产构建通过，嵌入布局检查通过。
- 构建服务使用真实 PostgreSQL 事务创建临时用户、指标树和各类内容，验证未确认、过期确认、子节点保护，以及确认后的级联清理和审计；全部测试数据最终回滚并核验无残留。
- 正式后端健康和前端入口、脚本摘要一致性通过。没有删除用户的实际指标数据，真实事务验证不代表本批重跑完整登录与浏览器删除操作。

本批同步修正两处验证基础：旧应用路由测试的 HeaderBar 替身需渲染默认插槽；公共 Schema 引用索引在已通过格式校验后补充非空类型断言，以适配知识库的严格 TypeScript 配置，运行逻辑不变。

前端发布包 SHA256：`a174574b7d378d021875c499ede63f229ce754161a5be6149fe54f6a2cdc6b40`。
