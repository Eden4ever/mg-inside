# 2026-09-08 统一认证生产发布

## 当前生产版本

- 统一认证：https://identity.meta-gravity.com ，镜像 `mg-identity-service:20260908-login-brand`。Vue 3 + Element Plus GUI 使用知识库原有 `mg-shell.css` 与 `primary-pages.css`；桌面导航 256px、顶栏 56px、页面内边距 24px、标题 18px、内容外阴影、固定标题与内容独立滚动。桌面折叠与移动抽屉已验证。
- Token One：https://token.meta-gravity.com ，镜像 `mg-gateway:identity-gui-20260908`（同时标记 latest），运行发布编号 `20260908T062000Z`，镜像 ID `sha256:a06c32d91e9ddcf9665f1bf2fd726198356e643236bf9852fb2fec67f8b71790`。
- 知识库：https://yshj.meta-gravity.com/knowledge-base-inside ，版本 `stage11-20260908-143500`。
- 两个业务应用 `IDENTITY_ENABLED=true`；业务本地认证入口和旧本地会话被拒绝，包括本地管理员。账号创建、身份资料、密码与 MFA 在中心维护；应用保留业务角色、分组、额度和历史数据。
- 按用户明确要求，将何子轩、刘雯婕已有中心身份设为 `system_admin`，已回读验证，未变更两人的业务角色。何子轩账号为 `hezixuan`；刘雯婕已绑定企微，尚未设置账号密码。

## 发布范围与恢复资料

中心执行 `20260908050000_central_provisioning`；Token One 只执行 `migration-add-identity.sql`，原有待执行日志保留索引迁移未顺带执行；知识库只新增 `20260908030000_identity_sso`。

知识库发版来源是 `mg-expert-database/artifacts/identity-release-work`，保留生产原有必填指标编码行为；工作区的自动指标编码改动未发布。具体暂存差异是恢复 `catalog.service.ts` 和 contracts 中原有必填编码契约，前端新增节点继续提交编码。原工作区未回滚。

- Token One 发布包 SHA-256：`bdaa5c15c21c63644aece4f974564dfbd7aec7b8170f73447c51a48cc3da8e70`。
- 知识库发布包 SHA-256：`778a6540ffe504a3802f6d1506bec392e6adc781fffa19402c0101859c3cb079`。
- 中心备份：`/var/backups/mg-identity/20260908-gui`，包括数据库、密钥与 compose。
- Token One 备份：`/opt/mg-gateway/backups/identity-gui-20260908`，包括数据库、部署文件和环境；另保留 `mg-gateway:rollback-identity-gui-20260908`。
- 知识库备份：`/opt/mg-expert-database/backups/stage11-20260908-143500`，包含数据库、原环境、认证加密密钥、服务和静态资源；旧版本目录仍保留。

Token One 首次切换曾因发布编号不符合 UTC 格式而短暂不可用，随后修正环境及镜像标签；最终就绪、发布编号与镜像均一致。恢复时应优先修复当前认证兼容版本，不得通过恢复旧单因素入口绕过中心停用和 MFA 策略。

## 验证证据

- 中心本地 API 3 项通过，覆盖两个真实 OIDC 客户端、授权码防重放、中心唯一建号、资料/停用同步、会话撤销和禅道已绑定身份策略；Linux 容器数据库、GUI 资源和管理鉴权通过。
- Token One 当前网关单元 224 项、真实数据库端到端 6 项、身份专项及真实身份投影 8 项通过。
- 知识库认证专用发版包后端 83 项通过；前端 119 项覆盖中曾有暂存编码测试预期调整，修正后该文件 8 项全部通过，其余 111 项已通过。构建和类型检查通过。
- 本地浏览器验证登录 Tab 清密码、创建身份、修改资料、授权、账号安全、桌面导航尺寸/折叠、移动抽屉和无横向溢出。
- 生产浏览器验证：从 Token One 自动进入中心，账号密码登录一次后进入知识库无需重复输入；中心修改姓名/部门经定时同步在两业务生效；从任一应用退出均使另一应用及中心当前会话失效；业务旧密码入口拒绝访问。
- 生产中心停用测试身份后，两业务数据库均同步为停用，映射唯一。测试身份及应用投影已精确清理，审计保留。
- 27 条历史映射逐项核对：Token One 11 条、知识库 16 条；原用户 ID、姓名、部门、已设置用户名、授权启停一致。
- 原有 Token One 12 个用户、知识库 19 个用户保留；两业务原角色保留；按用户明确要求关闭何子轩的知识库遗留 MFA，其他 MFA 标记不变。Token One 分组、固定月额度、总额度一致，已用额度未减少。知识库仍为 2 个体系、10 个节点、5 条研究记录、30 个模块、3 条修订、1 条依据、16 条体系授权。
- 生产 GUI 实测导航 256px、顶栏 56px、间距 24px、标题 18px，外阴影存在；配置与账号安全页面加载正常。

完整带账号标识的核对报告与浏览器截图在认证项目受限 `private` 目录，不提交源码。真实员工企业微信扫码和真实禅道密码未代用户操作；已验证官方扫码入口、回调白名单、可信 IP 和服务启用状态。认证策略与 MFA 由中心统一执行；知识库不再使用本地遗留 MFA 标记额外拒绝已通过中心认证的 SSO 会话。该行为后端测试通过。

登录页品牌和浏览器标题统一为 Auth One。已删除登录表单上方的三行介绍和卡片下方的账号说明，本地与生产浏览器验证通过，保留登录方式与必要操作提示。

按最新界面要求，logo 与 Auth One 已移入登录卡片顶部，下方新增「元引统一身份认证」。本地及生产桌面、移动端布局与登录 Tab 检查通过。
